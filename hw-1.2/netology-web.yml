apiVersion: v1
kind: Pod
metadata:
  name: netology-web
  labels:
    env: stage
    use: echoserver
spec:
  containers:
    - name: echoserver
      image: gcr.io/kubernetes-e2e-test-images/echoserver:2.2
